// This file is created once by gopernicus and will NOT be overwritten.
// Customize entity types by adding methods or fields.

package users

import (
	"time"

	"github.com/gopernicus/gopernicus/infrastructure/events"
)

// User is the domain entity for a user.
type User struct{ GeneratedUser }

// CreateUser is the input for creating a user.
// Defined manually because user creation is not generated — it requires
// a transactional principal insert handled by the custom store.
type CreateUser struct {
	UserID        string     `json:"user_id,omitempty"`
	Email         string     `json:"email,omitempty"`
	DisplayName   *string    `json:"display_name,omitempty"`
	EmailVerified bool       `json:"email_verified,omitempty"`
	LastLoginAt   *time.Time `json:"last_login_at,omitempty"`
	RecordState   string     `json:"record_state,omitempty"`
}

// UpdateUser is the input for updating a user.
type UpdateUser struct{ GeneratedUpdateUser }

// UserCreatedEvent is emitted when a new user is created.
type UserCreatedEvent struct {
	events.BaseEvent
	User User `json:"user"`
}
